# Message for defining a goal, which defines both the starting state region
# and the goal state region
# For each, there is a mask which is true for each variable we care about

float32[] start_state
bool[]    start_mask
float32[] goal_state
bool[]    goal_mask
